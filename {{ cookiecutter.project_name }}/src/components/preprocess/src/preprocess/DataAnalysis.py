# TODO: categorical description
# get categorical columns -> df.select_dtypes(include=['category', 'object']).columns
# check n_nulls -> df["column"].isna().sum()
# check n_columns -> df["column"].nunique()
